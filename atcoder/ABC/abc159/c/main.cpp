#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i, n) for(int i = 0; i < (n); ++i)


int main(){
	    int L; cin >> L;
        cout << fixed;
        cout << setprecision(12);
        cout << pow(L, 3) / 27 << endl;
}
