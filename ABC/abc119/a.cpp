#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i,n) for(int i = 0; i < (n); ++i)

int main(){
	int y, m, d;
	char c1, c2;
	cin >> y >> c1 >> m >> c2 >> d;
	cout << (m <= 4 ? "Heisei" : "TBD") << endl;
}
