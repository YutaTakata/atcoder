#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i, n) for(int i = 0; i < (n); ++i)

int main(){
  int x, a;
  cin >> x >> a;
  if(x < a) cout << 0 <<endl;
  else cout << 10 << endl;
  return 0;
}
